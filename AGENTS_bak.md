1. 项目目标（Project Goal）

本项目是一个基于 Web 的简化 CAD 建模工具，主要目标是：

支持硬件工程师在 Web 端创建异形单板（非规则形状），包括外轮廓建模与挖孔等操作。

项目重点：

提供 二维草图 → 三维实体建模 的完整流程

支持 面向团队使用场景的多人协作能力（增强能力）

使用 TypeScript 全栈实现

强调 可实现性优先于复杂功能与体验

该项目同时用于展示开发者的：

前端复杂交互能力（类 CAD 编辑器）

基本全栈能力（实时协作 + 后端服务）

2. 核心设计原则（Core Principles）
2.1 MVP 优先

优先保证功能可实现、可运行、可演示

避免一开始实现复杂几何或完整 CAD 能力

2.2 简化优先

统一规则优于复杂交互

降低用户自由度以换取系统稳定性

2.3 明确主链路

核心流程必须稳定：

创建平面 → 二维绘制 → 自动识别闭合区域 → 拉伸 → 生成实体 → 面上继续编辑 → 挖孔
2.4 协作是增强能力

多人协作不是核心建模前提

但符合 Web 工具团队使用场景

3. 技术栈（Tech Stack）
前端

React

TypeScript

Three.js

状态管理：自定义或轻量方案

后端

Hono.js（TypeScript）

WebSocket（用于协作）

约束

前后端统一使用 TypeScript

尽量共享类型定义

4. 核心概念模型（Core Domain Model）
4.1 工作平面（WorkPlane）

无限延伸的二维平面

所有二维图元的容器

用户通过创建平面开始建模

4.2 二维图元（2D Primitives）

第一版仅支持：

Line（线段）

Circle（圆）

特性：

所有图元依附于某个工作平面

可在后续编辑中被修改（移动、删除等）

4.3 闭合区域（Profile）

系统在退出二维编辑模式后：

自动从所有图元中识别闭合区域

仅识别无歧义闭合结构

支持：

线段闭环

单个圆

外轮廓 + 内孔（包含关系）

不支持（第一版）：

复杂相交图形自动分割

多图形布尔运算

规则：

无法形成合法闭合区域的图元将被忽略，但不会删除

4.4 三维实体（Solid）

由闭合区域通过拉伸生成：

支持正向拉伸（生成实体）

支持反向拉伸（切除 → 挖孔）

5. 编辑模式（Editing Modes）

系统存在两种模式：

5.1 三维模式（3D Mode）

默认模式：

可查看三维场景

可创建工作平面

可选择实体或面

鼠标 hover 可高亮闭合区域

5.2 二维编辑模式（2D Sketch Mode）

进入方式：

选中工作平面

或选中实体的某个平整面

行为：

所有操作仅作用于当前平面

可绘制/编辑二维图元

视角规则：

自动切换为该平面的正交视图

退出时恢复三维视角

6. 拉伸规则（Extrusion Rules）
6.1 触发方式

在三维模式下，对某个平面执行拉伸

6.2 作用范围

拉伸作用于当前平面上所有可识别闭合区域

6.3 参数规则

所有区域使用统一参数

高度

方向（正向 / 反向）

6.4 行为结果

正向：生成实体

反向：对现有实体进行切除（挖孔）

7. 工作平面生命周期（WorkPlane Lifecycle）
拉伸前：

平面存在

图元可编辑

拉伸后：

工作平面被删除

其上的二维图元全部销毁

生成三维实体

说明：

实体的平整面可重新进入二维编辑

这些面等价于新的工作平面（对用户隐藏）

8. 协作设计（Collaboration - MVP）

目标：

引入后端能力

支持基础多人协作

第一版范围：

房间级协作

二维图元数据同步

基本实时更新

不要求：

完整 OT / CRDT

精细冲突解决

策略：

简单状态同步

后端作为中转 + 广播

9. 约束系统（Constraint System - 简化版）

采用轻量策略：

通过“多轮迭代修复”的方式逐步满足约束

机制：

遍历所有约束

每个约束尝试修正相关图元

多轮迭代后趋近稳定

第一版目标：

不追求严格数学解

优先“看起来合理”

10. 明确不做（Out of Scope for MVP）

以下内容不属于第一版：

复杂几何布尔运算

完整约束求解器

历史树 / 参数化回溯

多实体装配系统

高级 CAD 功能（倒角、圆角等）

高级冲突协作（CRDT/OT）

11. 项目定位总结（Positioning）

该项目不是完整 CAD 软件，而是：

一个面向异形单板设计的、Web 化的简化建模工具，重点展示二维草图建模、三维生成以及基础协作能力。

12. 开发优先级（Development Priority）

优先级从高到低：

二维绘制能力（线段、圆）

闭合区域识别

拉伸生成三维实体

面上编辑 + 挖孔

基础协作能力

约束系统（基础版）

体验优化

13. 成功标准（Success Criteria）

项目完成应满足：

能完整演示：
画轮廓 → 拉伸 → 在面上画圆 → 挖孔

系统稳定运行

支持基础多人协作

有清晰代码结构与类型定义

可用于简历展示与面试讲解