# AGENTS.md

## 1. 项目目标（Project Goal）

本项目是一个基于 Web 的简化 CAD 建模工具，目标是：

* 支持硬件工程师在 Web 端创建异形单板（非规则形状）
* 支持外轮廓建模与挖孔操作

### 项目重点

* 提供 **二维草图 → 三维实体建模** 的完整流程
* 支持团队场景下的**多人协作能力（增强能力）**
* 使用 **TypeScript 全栈实现**
* 强调 **可实现性优先于复杂功能与体验**

### 项目价值

用于展示开发者能力：

* 前端复杂交互（类 CAD 编辑器）
* 基本全栈能力（实时协作 + 后端服务）

---

## 2. 核心设计原则（Core Principles）

### 2.1 MVP 优先

* 保证功能可运行、可演示
* 避免一开始实现复杂 CAD 能力

### 2.2 简化优先

* 统一规则优于复杂交互
* 降低用户自由度以提高稳定性

### 2.3 明确主链路

核心流程必须稳定：

```
创建平面 → 二维绘制 → 识别闭合区域 → 拉伸 → 生成实体 → 面上编辑 → 挖孔
```

### 2.4 协作是增强能力

* 非核心建模前提
* 用于提升 Web 工具价值

---

## 3. 技术栈（Tech Stack）

### 前端

* React
* TypeScript
* Three.js
* 状态管理：自定义或轻量方案（如 Zustand）

### 后端

* Hono.js（TypeScript）
* WebSocket（用于协作）

### 约束

* 前后端统一使用 TypeScript
* 尽量共享类型定义（建议抽 shared package）
* ts 类型优先用 type 而不是 interface
* 代码多用函数而不是类, 尤其是适合写单元测试的纯函数
* git commit message 格式, 第一行是:`[随机emoji] [简短总结]`, 接着一个空白换行, 从第三行开始分点概括改动内容, 不超过 5 点

---

## 4. 核心概念模型（Core Domain Model）

### 4.1 工作平面（WorkPlane）

* 无限二维平面
* 所有二维图元的容器
* 建模起点

### 4.2 二维图元（2D Primitives）

第一版支持：

* Line（线段）
* Circle（圆）

特性：

* 依附于某个 WorkPlane
* 支持编辑（移动、删除）

### 4.3 闭合区域（Profile）

系统在退出二维编辑模式后自动识别：

支持：

* 线段闭环
* 单个圆
* 外轮廓 + 内孔

不支持：

* 复杂相交图形自动分割
* 布尔运算

规则：

* 无法闭合的图元将被忽略（不删除）

### 4.4 三维实体（Solid）

由 Profile 拉伸生成：

* 正向拉伸 → 生成实体
* 反向拉伸 → 切除（挖孔）

---

## 5. 编辑模式（Editing Modes）

### 5.1 三维模式（3D Mode）

默认模式：

* 浏览三维场景
* 创建工作平面
* 选择实体或面
* hover 高亮闭合区域

### 5.2 二维编辑模式（2D Sketch Mode）

进入方式：

* 选择工作平面
* 或选择实体平面

行为：

* 仅操作当前平面
* 支持绘制/编辑图元

视角规则：

* 自动切换为正交视图
* 退出时恢复三维视角

---

## 6. 拉伸规则（Extrusion Rules）

### 6.1 触发方式

* 在 3D 模式下执行

### 6.2 作用范围

* 当前平面所有闭合区域

### 6.3 参数

* 高度
* 方向（正 / 反）

### 6.4 结果

* 正向 → 新实体
* 反向 → 挖孔

---

## 7. 工作平面生命周期（WorkPlane Lifecycle）

### 拉伸前

* 平面存在
* 图元可编辑

### 拉伸后

* WorkPlane 删除
* 图元销毁
* 生成 Solid

说明：

* 实体表面可重新进入编辑
* 等价于隐式 WorkPlane

---

## 8. 协作设计（Collaboration - MVP）

目标：

* 支持基础多人协作

范围：

* 房间级协作
* 二维图元同步
* 基础实时更新

不包含：

* OT / CRDT
* 精细冲突解决

策略：

* 后端中转 + 广播
* 简单状态同步

---

## 9. 约束系统（Constraint System - 简化版）

策略：

* 多轮迭代修复

机制：

1. 遍历约束
2. 修正图元
3. 多轮逼近稳定

目标：

* 不追求严格数学解
* 优先视觉合理性

---

## 10. 不做（Out of Scope）

* 复杂布尔运算
* 完整约束求解器
* 历史树 / 参数化回溯
* 装配系统
* 高级 CAD 功能（倒角等）
* 高级协作（CRDT/OT）

---

## 11. 项目定位（Positioning）

不是完整 CAD，而是：

> 一个面向异形单板设计的 Web 简化建模工具
> 聚焦草图建模、三维生成与基础协作

---

## 12. 开发优先级（Development Priority）

1. 二维绘制（线 / 圆）
2. 闭合区域识别
3. 拉伸建模
4. 面上编辑 + 挖孔
5. 协作能力
6. 约束系统
7. 体验优化

---

## 13. 成功标准（Success Criteria）

项目完成需满足：

* 可完整演示流程：

  ```
  画轮廓 → 拉伸 → 面上画圆 → 挖孔
  ```
* 系统稳定运行
* 支持基础多人协作
* 代码结构清晰
* 可用于面试展示
